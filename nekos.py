# Compatibility stub for the `nekos` module.
#
# The cogs carry a leftover top-level `import nekos` but never call it
# (no `nekos.*` references exist in the code), so this empty module only
# exists to satisfy the import. Replace with the real library/module if a
# future cog actually uses it.
